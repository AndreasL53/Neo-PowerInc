apiVersion: batch/v1
kind: Job
metadata:
  namespace: example05
  name: stop-altering
  annotations:
    argocd.argoproj.io/sync-wave: "-25" 
spec:
  template:
    spec:
      containers:
      - name: dummy-run
        image: alpine
        command:
          - "sleep"
          - "10"
      restartPolicy: Never
  backoffLimit: 2    
